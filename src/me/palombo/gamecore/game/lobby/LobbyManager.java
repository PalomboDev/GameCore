package me.palombo.gamecore.game.lobby;

import me.palombo.gamecore.module.Module;
import org.bukkit.plugin.java.JavaPlugin;

public class LobbyManager extends Module {

    public LobbyManager(JavaPlugin plugin) {
        super(plugin);
    }
}
